# Local data mount

This directory is intentionally empty in the release. Build
`osm_prepared_input.npz` from authorized, already prepared tables using
`scripts/prepare_inputs.py` and the contract in `docs/DATA_CONTRACT.md`.

Do not commit ground truth, reference cells, target expression, coordinates,
or pseudo-spots unless their redistribution terms explicitly permit it.
