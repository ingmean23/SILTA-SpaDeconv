# Required artifacts

This directory is intentionally ready but contains no model data yet.

Required filenames:

```text
silta_osm_b_fold42_seed42.model
silta_osm_b_fold42_seed42_c04.pt
```

The calibration bundle schema is documented in
`../docs/CALIBRATION_STATE_CONTRACT.md`. After adding both files, regenerate
`MANIFEST.sha256` and rerun the security and release validators.
